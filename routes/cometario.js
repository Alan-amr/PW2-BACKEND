const express = require("express");
//require("sequelize") = require("../validators/usuario")
//const { getAllUsers, getUser, login, createUser, UpdateUser} = require("../controllers/comentario");
const router = express.Router();

//TODO http://localhost/calificacion GET,POST,DELETE,PUT
/*
router.get("/",getAllUsers);
router.get("/:id",getUser);
router.get("/:email/:password",login);
router.post("/",createUser);
router.put("/:id",UpdateUser);*/

module.exports=router